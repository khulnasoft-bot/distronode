{
    scopeName = 'injection.distronode.jinja-double-quote-escape';
    injectionSelector = 'L:string.quoted.double meta.embedded.inline.jinja';
    name = 'Injection grammar for supporting double quote escapes in Jinja2 expressions';
    patterns = (
        {
            name = 'string.quoted.double.jinja';
            match = '(\\")(.*?)(\\")';
            captures = {
                1 = {
                    name = 'constant.character.escape.double-quoted.distronode';
                };
                2 = {
                    patterns = (
                        { include = 'source.distronode-jinja#string'; }
                    );
                };
                3 = {
                    name = 'constant.character.escape.double-quoted.distronode';
                };
            };
        },
        {
            name = 'invalid.illegal.expected-escaped-double-quote.distronode';
            comment = (
                'This technique will remind the user to use quote escaping'
            );
            match = '(?x)
                "(?! \s+ (?=\#|$)) # we accept the quote only if the line ends or ends with a comment
            ';
        }
    );
}
