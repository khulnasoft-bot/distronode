bugfixes:
  - distronode-test - Always use distronode-test managed entry points for distronode-core CLI tools when not running from source.
                   This fixes issues where CLI entry points created during install are not compatible with distronode-test.
