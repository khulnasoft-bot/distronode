---
minor_changes:
- >-
  Add ``GALAXY_COLLECTIONS_PATH_WARNING`` option to disable the warning
  given by ``distronode-galaxy collection install`` when installing a collection
  to a path that isn't in the configured collection paths.
