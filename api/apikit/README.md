apikit
======

A Python library that backs the provided `api` command line client.

It can be installed by running `pip install apikit`.

The PyPI respository can be found [here](https://pypi.org/project/apikit/).

For more information on installing the CLI and building the docs on how to use it, look [here](./apikit/cli/docs).