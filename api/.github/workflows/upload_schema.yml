---
name: Upload API Schema

env:
  LC_ALL: "C.UTF-8" # prevent ERROR: Distronode could not initialize the preferred locale: unsupported locale setting

on:
  push:
    branches:
      - devel
      - release_**
      - feature_**
jobs:
  push:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          show-progress: false

      - name: Get python version from Makefile
        run: echo py_version=`make PYTHON_VERSION` >> $GITHUB_ENV

      - name: Install python ${{ env.py_version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.py_version }}       

      - name: Log in to registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Pre-pull image to warm build cache
        run: |
          docker pull -q ghcr.io/${{ github.repository_owner }}/api_devel:${GITHUB_REF##*/} || :

      - name: Build image
        run: |
          DEV_DOCKER_TAG_BASE=ghcr.io/${{ github.repository_owner }} COMPOSE_TAG=${GITHUB_REF##*/} make docker-compose-build

      - name: Generate API Schema
        run: |
          docker run -u $(id -u) --rm -v ${{ github.workspace }}:/api_devel/:Z \
            --workdir=/api_devel ghcr.io/${{ github.repository_owner }}/api_devel:${GITHUB_REF##*/} /start_tests.sh genschema

      - name: Upload API Schema
        env:
          AWS_ACCESS_KEY: ${{ secrets.AWS_ACCESS_KEY }}
          AWS_SECRET_KEY: ${{ secrets.AWS_SECRET_KEY }}
          AWS_REGION: 'us-east-1'
        run: |
          distronode localhost -c local, -m command -a "{{ distronode_python_interpreter + ' -m pip install boto3'}}"
          distronode localhost -c local -m aws_s3 \
            -a "src=${{ github.workspace }}/schema.json bucket=api-public-ci-files object=${GITHUB_REF##*/}/schema.json mode=put permission=public-read"


